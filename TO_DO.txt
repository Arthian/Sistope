Principal:
	Obtener parámetros de entrada
	Tamaño archivo/NumeroDeHebras
	Echar hebras a correr con atributo tid {0,1,2,3...}
	esperar hebras.

Lector:
	While true
	{
		bloquear mutex tabla
		bloquear mutex contador
		contador++
		desbloquear contador
		desbloquear tabla
		leer
		bloquear contador
		contador--
		desbloquear contador
		//TERMINO.
	}

Escritor:
	Calcular donde empezar en funcion del tid.
	Abrir el archivo en la posicion correspondiente
	while(deba seguir leyendo){
		leer palabra
		calcular hash
		calcular posicion
		bloquear tabla
			if(contador==0){
			escribir donde corresponda, encolar en la lista
			calcular si se supera el threshold
				Si:
					resize
			}
			sino dormir hasta que se vacie.
		desbloquear tabla
	}


Resize:
	{
	tomar latabla con n listas.
	crear una tabla temporal con 2n listas
	para cada palabra en las listas en la tabla (for{for}
	calcular hash, calcular nueva posicion, guardar.
	tabla = tabla temporal.
	}

lecturaParametrosLector:
	splitear todo por espacio, meterlo en una matriz de strings
	comparar si i es un comando (-p, -n, etc). Si es asi obtener el parametro de i+1